epochs: 40
batch_size: 8
learning_rate: 0.001
weight_decay: 0.0001
gradient_clip_norm: 1.0
gradient_accumulation_steps: 1
random_rotation: true
mixed_precision: true
event_loss: class_balanced_cross_entropy
event_classes:
  - WAIT
  - EXTEND
  - SPLIT
  - STOP
